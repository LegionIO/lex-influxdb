# frozen_string_literal: true

RSpec.describe Legion::Extensions::Influxdb do
  it 'has a version number' do
    expect(Legion::Extensions::Influxdb::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
